<div class="col-12">
                            <div class="form-group">
                                <label for="<%-prop.name%>"><%-prop.name.Ab()%></label>
                                <select class="form-control" name="<%-prop.name%>[]" id="<%-prop.name%>" multiple style="width: 100%" <%= from == 'show' ? 'disabled' : '' %>>
                                    @foreach($<%-prop.model.table%> as $<%-prop.model.lower%>)
                                    <option value="{{$<%-prop.model.lower%>->id}}" {{ (in_array($<%-prop.model.lower%>->id, old('<%-prop.model.table%>', [])) <%-from != 'create' ? `|| $${prop.root.lower}->${prop.model.table}->contains($${prop.model.lower}->id)` : ''%>) ? 'selected' : '' }}>{{$<%-prop.model.lower%>->name}}</option>
                                    @endforeach
                                </select>
                            </div>
                        </div>