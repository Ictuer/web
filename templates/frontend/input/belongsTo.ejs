<div class="col-12">
    <div class="form-group">
        <label for="<%-prop.model.id%>"><%-prop.model.name%></label>
        <select class="form-control" name="<%-prop.model.id%>" id="<%-prop.model.id%>" style="width: 100%" <%= from == 'show' ? 'disabled' : '' %>>
            @foreach($<%-prop.model.table%> as $<%-prop.model.lower%>)
            <option value="{{$<%-prop.model.lower%>->id}}" {{ ($<%-prop.model.lower%>->id == old('<%-prop.model.id%>') <%-from != 'create' ? `|| $${prop.root.lower}->${prop.model.lower}->id == $${prop.model.lower}->id` : ''%>) ? 'selected' : '' }}>{{$<%-prop.model.lower%>->name}}</option>
            @endforeach
        </select>
    </div>
</div>